#! /bin/bash
# 创建临时文件夹，方便整理
mkdir /tmp/临时文件            # create temporary folder
mkdir /tmp/临时文件/QQ         # create temporary folder for QQ
mkdir /tmp/临时文件/browser    # create temporary folder for browser like chrome
mkdir /tmp/临时文件/screenshot # create temporary folder for screenshot
mkdir /tmp/临时文件/aria2      # create temporary folder for BT
